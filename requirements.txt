# requirements.txt
# Install with: pip install -r requirements.txt
# For GPU acceleration on Apple Silicon, install PyTorch with MPS support:
#   pip install torch torchvision torchaudio
# For NVIDIA CUDA:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

torch>=2.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
rich>=13.0.0
